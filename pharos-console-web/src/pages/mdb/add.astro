---
/* ========================================================================
 * Project: pharos
 * Component: Web Console
 * File: src/pages/mdb/add.astro
 * Author: Richard D. (https://github.com/iamrichardd)
 * License: AGPL-3.0 (See LICENSE file for details)
 * * Purpose (The "Why"):
 * Page for adding machine records. Uses Vertical Slice Architecture
 * to delegate to the MdbAddForm component.
 * * Traceability:
 * Related to Task 16.6 (Issue #69).
 * ======================================================================== */
import ConsoleLayout from '../../layouts/ConsoleLayout.astro';
import MdbAddForm from '../../features/mdb/add/MdbAddForm.astro';
import { actions } from 'astro:actions';

const result = Astro.getActionResult(actions.addMachine);
---

<ConsoleLayout title="Add Machine Record">
    <div class="space-y-6 max-w-2xl mx-auto">
        <div>
            <h1 class="text-2xl font-bold text-slate-900 sm:text-3xl">Add Machine Record</h1>
            <p class="text-slate-600">Register a new node in the infrastructure database.</p>
        </div>

        <MdbAddForm result={result} />
    </div>
</ConsoleLayout>
